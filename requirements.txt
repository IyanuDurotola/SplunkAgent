# FastAPI & Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Splunk Integration
splunk-sdk==2.1.1
splunklib==1.0.0

# LLM & AI (Amazon Bedrock)
boto3==1.34.0
botocore==1.34.0

# Vector DB & RAG (PostgreSQL with pgvector)
# Note: pgvector is a PostgreSQL extension, not a Python package
# Install it in PostgreSQL: CREATE EXTENSION vector;
asyncpg==0.29.0
psycopg2-binary==2.9.9
sqlalchemy[asyncio]==2.0.23
# Embeddings are now handled by Amazon Bedrock Titan (no local dependencies needed)

# Data Processing
pandas==2.1.3
numpy==1.26.2
python-dateutil==2.8.2

# Utilities
python-dotenv==1.0.0
httpx==0.25.2
aiohttp==3.9.1

# Logging & Monitoring
structlog==23.2.0

# Testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
requests==2.31.0

